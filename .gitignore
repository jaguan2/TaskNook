# Python
__pycache__/
*.pyc
.venv/
venv/
backend/tasknook.db

# PyInstaller (desktop .exe packaging) — build artifacts only;
# TaskNook.exe at the repo root IS tracked on purpose, so GitHub
# visitors can download and run it directly.
/build/
*.spec

# Node / Vite
node_modules/
frontend/dist/
*.local

# Editor / OS
.DS_Store
.vscode/
