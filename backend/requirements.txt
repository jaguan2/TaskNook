Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
# Pinned explicitly, not left to Flask-Migrate: schema.py imports alembic
# directly (MigrationContext / ScriptDirectory), so it's a first-party
# dependency. An unpinned major could break the packaged .exe silently.
alembic==1.13.2
# 3.0.6+ carries the safe_join/multipart hardening patch releases
# (CVE-2024-49766/49767) — relevant because the app serves files on Windows.
Werkzeug==3.0.6
